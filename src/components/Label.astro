---
interface Props {
    title: string
}
const { title } = Astro.props;
---
<p class="text-[56px] md:text-[104px] leading-[110%]  font-extrabold italic"><span class="text-purple" id={`lbl${title}`}> -- </span> {title}</p>